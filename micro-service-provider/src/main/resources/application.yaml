spring:
  application:
    name: micro-provider
  cloud:
    zookeeper:
      connect-string: localhost:2181
      discovery:
        register: true
        enabled: true
        instance-id: 2
        root: /app-zk-server
server:
  port: 6062
